Load robot info
---
tags:
  - Robot
parameters:
  - name: robot_id
    in: path
    description: The ID of the robot to retrieve
    required: true
    schema:
      type: integer
      format: int32

responses:
  200:
    description: When the robot data is successfully retrieved
    content:
      application/json:
        schema:
          type: object
          properties:
            # Add properties here to match the structure of the robot_info response
            # For example:
            # id:
            #   type: integer
            #   format: int32
            # name:
            #   type: string

  404:
    description: Fails to get robot data
